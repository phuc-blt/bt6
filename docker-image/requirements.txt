# FastAPI and dependencies
fastapi==0.95.2
uvicorn==0.23.2

# YOLOv5 (YOLOv11 có thể được sử dụng qua ultralytics/yolov5)
torch==2.1.0
torchvision==0.15.0

# Bing Image Downloader
bing-image-downloader==1.0.4

# Airflow and dependencies
apache-airflow==2.7.0
apache-airflow-providers-sqlite==2.7.0  # Replace with the appropriate provider if you're using a different DB

# For image processing and handling
pillow==9.4.0
requests==2.28.2
opencv-python-headless==4.6.0.66

# Additional libraries for handling JSON and model saving
pydantic==1.10.5
joblib==1.2.0
scipy==1.10.1

# Optional: For logging and debugging
python-dotenv==1.0.0

# For file handling (useful for exporting and importing models)
shutil==1.0.0
